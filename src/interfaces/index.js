/**
 * TypeScript interfaces (nếu dự án sử dụng TypeScript)
 * Hoặc có thể dùng JSDoc để type hint trong JavaScript
 */

/**
 * @typedef {Object} UserPayload
 * @property {string} userId
 * @property {string} email
 * @property {string} username
 */

/**
 * @typedef {Object} RequestWithUser
 * @property {UserPayload} user
 */

module.exports = {};

